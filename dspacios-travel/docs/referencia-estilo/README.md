# Referencia de estilo — sitio coral-gull

Sube aquí **capturas de pantalla** (PNG/JPG) del sitio cuyo estilo quieres
replicar (`coral-gull-161284.hostingersite.com`). No puedo abrir la URL desde
el entorno (la política de red de la sesión bloquea ese host), pero **sí leo
imágenes**, así que con las capturas extraigo los colores y tipografías.

## Qué subir (ideal 3–5 capturas)

```
dspacios-travel/docs/referencia-estilo/01-inicio.png        (la home / hero)
dspacios-travel/docs/referencia-estilo/02-seccion.png       (una sección con fondo de color)
dspacios-travel/docs/referencia-estilo/03-boton.png         (un botón / CTA de cerca)
dspacios-travel/docs/referencia-estilo/04-titulo.png        (un título grande)
```

Cuando estén arriba, avísame: te devuelvo la **paleta detectada** (fondo,
títulos, acentos, botones — con HEX aproximados) y cómo aplicarla a los tokens
de la app (`styles/globals.css`), respetando o ajustando el manual de marca.
